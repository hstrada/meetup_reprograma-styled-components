import { DefaultTheme } from 'styled-components';

import colors from './colors';

import units from './units';

const styledeTheme: DefaultTheme = {
  colors: colors,
  units: units,
};

export { styledeTheme };
